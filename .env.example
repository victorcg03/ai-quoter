# ==== LOGGING ====
# debug | info | error
LOG_LEVEL=info
# Filtrar por namespaces: p. ej. "AGENT,SUGGEST,QUOTE,PDF,OLLAMA"
LOG_NS=

# ==== OLLAMA (IA local) ====
# URL del servidor de Ollama accesible desde el contenedor/host
# En local suele ser http://localhost:11434
# En Coolify/Docker, usa el nombre del servicio si está en la misma red, p.ej. http://ollama:11434
OLLAMA_URL=http://localhost:11434
# Timeout de las llamadas al chat (ms)
OLLAMA_TIMEOUT_MS=25000

# Puerto (Coolify suele inyectar PORT automáticamente)
# PORT=3000
